import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'gh_grabber_android',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
